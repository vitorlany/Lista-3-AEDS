#include <stdio.h>
#include <stdlib.h>

int main()
{
    float inserido = 0, lista[];

    while (1) {
        printf("Insira os numeros: ");
        scanf("%f", inserido);

        if (inserido != -1 ) {

        } else {
            break;
        }
    }

    return 0;
}
